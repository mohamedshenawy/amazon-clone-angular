import { ChangeproductimgDirective } from './changeproductimg.directive';

describe('ChangeproductimgDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeproductimgDirective();
    expect(directive).toBeTruthy();
  });
});
